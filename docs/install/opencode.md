# OpenCode

Prefer project or user skill directories documented for your OpenCode build (often under agent config or `.opencode` / skills paths compatible with Agent Skills).

```bash
cp -R skills/* <opencode-skills-root>/
```

If OpenCode supports `npx skills add <owner/repo>`, point it at this repository once published.

Invoke via slash commands or skill descriptions.
