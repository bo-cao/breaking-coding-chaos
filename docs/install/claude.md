# Claude Code

**User skills:** copy into `~/.claude/skills/<name>/`  
**Project skills:** `.claude/skills/<name>/` in the repo  

```bash
cp -R skills/* ~/.claude/skills/
```

Optional: Claude plugin marketplace later; v1 is plain skill folders.

Invoke: `/breaking-coding-chaos`, `/hardpoint`, `/plan-spar`, or natural language.
