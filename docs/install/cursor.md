# Cursor

**User:** `~/.cursor/skills/<name>/`  
**Project:** `.cursor/skills/<name>/`

```bash
cp -R skills/* ~/.cursor/skills/
```

Reload window if skills do not appear. Invoke via slash skills or chat triggers in skill `description` fields.
