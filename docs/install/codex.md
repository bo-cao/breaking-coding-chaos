# Codex

Common skill roots (check your Codex version docs):

- `~/.agents/skills/`
- `~/.codex/skills/`

```bash
cp -R skills/* ~/.agents/skills/
```

Invoke via Codex skills UI / `$skill-name` / natural language matching descriptions.
