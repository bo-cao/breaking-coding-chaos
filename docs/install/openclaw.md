# OpenClaw

Install skills into OpenClaw’s skill or extension path per current OpenClaw documentation.

```bash
cp -R skills/* <openclaw-skills-root>/
```

Restart OpenClaw if required. Trigger via skill entry or natural language matching each skill’s `description`.
