# Hermes

Copy skill folders into Hermes’ configured skills / plugins directory (see current Hermes docs for `~/.hermes` layout).

```bash
cp -R skills/* ~/.hermes/skills/   # path may vary by version
```

Confirm with Hermes skill list after restart. Chat or slash invoke `breaking-coding-chaos` / `plan-spar`.
